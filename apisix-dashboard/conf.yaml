conf:
  listen:
    host: 0.0.0.0
    port: 9000

  etcd:
    endpoints:
      - "http://etcd:2379"
    prefix: "/apisix"
    timeout: 5

  log:
    error_log:
      level: warn

  authentication:
    secret: "ganti-ini-dengan-string-random-yang-panjang-biar aman"
    expire_time: 3600
    users:
      - username: "admin"
        password: "ganti_password_dashboard_yang_kuat"
